'use client';

export const SearchProvider = ({ children }: { children: React.ReactNode }) => {
  // Simplified search provider - can be enhanced later with actual search functionality
  return <>{children}</>;
};
